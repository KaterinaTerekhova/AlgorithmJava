public class BiggerValue {

    public static int biggerValueAlgorithm(int numA, int numB) {
        if (numA > numB) {
            return numA;
        } else {
            return numB;
        }
    }
}
